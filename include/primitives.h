#ifndef SAGE_PRIMITIVES_H
#define SAGE_PRIMITIVES_H

/*#include <stddef.h>
#include <stdbool.h>
#include <inttypes.h>
#include <threads.h>


struct sage_point_t {
    float x;
    float y;
};


struct sage_area_t {
    uint16_t h;
    uint16_t w;
};


enum sage_compare_t {
    SAGE_COMPARE_LT = -1,
    SAGE_COMPARE_EQ = 0,
    SAGE_COMPARE_GT = 1
};*/


#endif /* SAGE_PRIMITIVES_H */

